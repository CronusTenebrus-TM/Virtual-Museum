using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arif_Message : MonoBehaviour
{
    public Rigidbody panel;
    public Rigidbody player;
    private Quaternion q;

    private void Update()
    {
        //panel.transform.LookAt(player.transform);
        //q = panel.transform.rotation;
        //q[0] = 90;
        //panel.transform.rotation = q;
    }
}
